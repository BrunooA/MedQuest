import 'package:flutter/material.dart';

class MedicacoesPage extends StatelessWidget {
  const MedicacoesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Medicações"),
    );
  }
}