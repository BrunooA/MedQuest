import 'package:flutter/material.dart';

class ExamesPage extends StatelessWidget {
  const ExamesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela de Exames"),
    );
  }
}