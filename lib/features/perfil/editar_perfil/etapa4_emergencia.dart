import 'package:flutter/material.dart';

class Etapa4Emergencia extends StatelessWidget {
  const Etapa4Emergencia({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Etapa 4 Emergencia')),
    );
  }
}