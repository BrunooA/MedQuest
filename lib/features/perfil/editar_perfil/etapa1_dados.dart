import 'package:flutter/material.dart';

class Etapa1Dados extends StatelessWidget {
  const Etapa1Dados({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Etapa 1 Dados')),
    );
  }
}