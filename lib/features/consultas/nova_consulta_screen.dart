import 'package:flutter/material.dart';

class NovaConsultaScreen extends StatelessWidget {
  const NovaConsultaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Nova Consulta')),
    );
  }
}