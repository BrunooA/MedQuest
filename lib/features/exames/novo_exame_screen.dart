import 'package:flutter/material.dart';

class NovoExameScreen extends StatelessWidget {
  const NovoExameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Novo Exame')),
    );
  }
}